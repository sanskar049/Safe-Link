services:
  - type: web
    name: safelink-detector
    runtime: python
    buildCommand: pip install -r requirements.txt && playwright install chromium
    startCommand: gunicorn app:app --workers 1 --threads 4 --timeout 90
